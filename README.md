# TO TEST

- [x] PR merge w/ changeset -> new version PR
- [x] new version PR merge -> tag created

# Gotchas

make sure gh repo has perms set for actions to create PRs

```
Go to Repo → Settings

Click Actions → General
```
